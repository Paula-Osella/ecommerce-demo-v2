---
import MainLayout from "../layouts/MainLayout.astro";

const collections = [
  {
    id: "nourish",
    name: "Nourish",
    description: "Rutinas nutritivas para pieles que necesitan confort.",
  },
  {
    id: "cleanse",
    name: "Cleanse",
    description: "Limpieza suave para preparar la piel para el resto de tu rutina.",
  },
  {
    id: "sets",
    name: "Sets",
    description: "Combos curados para regalar o armar tu ritual completo.",
  },
];

const featuredProducts = [
  {
    id: 1,
    name: "Sérum hidratante",
    category: "Nourish",
    price: "$25.000",
    description: "Hidratación intensa para todo tipo de piel.",
    image: "/Product_Carrousel/Product_Carrousel_1.webp",
  },
  {
    id: 2,
    name: "Crema de noche nutritiva",
    category: "Nourish",
    price: "$30.000",
    description: "Repara y nutre mientras dormís.",
    image: "/Product_Carrousel/Product_Carrousel_1.webp",
  },
  {
    id: 3,
    name: "Limpiador facial suave",
    category: "Cleanse",
    price: "$18.000",
    description: "Limpieza diaria sin resecar la piel.",
    image: "/Product_Carrousel/Product_Carrousel_1.webp",
  },
  {
    id: 4,
    name: "Espuma limpiadora",
    category: "Cleanse",
    price: "$20.000",
    description: "Textura ligera, sensación de piel fresca.",
    image: "/Product_Carrousel/Product_Carrousel_1.webp",
  },
  {
    id: 5,
    name: "Contorno de ojos",
    category: "Youth",
    price: "$22.000",
    description: "Ilumina y suaviza la zona del contorno.",
    image: "/Product_Carrousel/Product_Carrousel_1.webp",
  },
  {
    id: 6,
    name: "Mascarilla nocturna",
    category: "Nourish",
    price: "$28.000",
    description: "Tratamiento intensivo para noches de spa en casa.",
    image: "/Product_Carrousel/Product_Carrousel_1.webp",
  },
];

const reviews = [
  {
    id: 1,
    name: "María G.",
    role: "Cliente frecuente",
    rating: 5,
    text: "Desde que uso la rutina completa noto la piel mucho más luminosa y sin tanta tirantez.",
  },
  {
    id: 2,
    name: "Lucía P.",
    role: "Piel sensible",
    rating: 4,
    text: "Por primera vez encontré productos que no me irritan. Me encanta la textura de los sérums.",
  },
  {
    id: 3,
    name: "Camila R.",
    role: "Nueva en skincare",
    rating: 5,
    text: "La web me ayudó a entender cómo armar mi ritual. Todo se siente simple y aplicable al día a día.",
  },
];

const looks = [
  {
    id: 1,
    title: "Natural Glow",
    description: "Look fresco y luminoso para el día a día",
    products: "Base + Rubor + Gloss",
    image: "/Looks/Looks_1.webp",
  },
  {
    id: 2,
    title: "Glam Night",
    description: "Dramático y sofisticado para la noche",
    products: "Smokey eyes + Labial mate",
    image: "/Looks/Looks_2.webp",
  },
  {
    id: 3,
    title: "Soft Romance",
    description: "Tonos rosados y suaves",
    products: "Sombras rosadas + Labial nude",
    image: "/Looks/Looks_3.webp",
  },
];

const instagramPosts = [
  {
    id: 1,
    likes: 234,
    video: "/Feed/Feed_2.mp4",
  },
  {
    id: 2,
    likes: 189,
    video: "/Feed/Feed_3.mp4",
  },
  {
    id: 3,
    likes: 412,
    video: "/Feed/Feed_2.mp4",
  },
  {
    id: 4,
    likes: 298,
    video: "/Feed/Feed_3.mp4",
  },
];

const blogPosts = [
  {
    id: 1,
    title: "Cómo elegir el tono perfecto de base",
    category: "Tutorial",
    date: "15 Nov 2024",
    image: "/Tendencia/Tendencia_1.webp",
  },
  {
    id: 2,
    title: "Tendencias de maquillaje para esta temporada",
    category: "Trends",
    date: "10 Nov 2024",
    image: "/Tendencia/Tendencia_2.webp",
  },
  {
    id: 3,
    title: "Rutina de skincare pre-maquillaje",
    category: "Skincare",
    date: "5 Nov 2024",
    image: "/Tendencia/Tendencia_3.webp",
  },
];
---

<MainLayout title="Inicio | Demo e-commerce">
  {/* NEWSLETTER POPUP - Se muestra automáticamente */}
  <div
    id="newsletter-popup"
    class="hidden fixed inset-0 bg-black/40 z-50 flex items-center justify-center p-4"
  >
    <div class="bg-white rounded-3xl max-w-md w-full p-8 relative animate-fade-in">
      <button
        id="close-popup"
        class="absolute top-4 right-4 text-zinc-400 hover:text-zinc-600 text-2xl"
      >
        ×
      </button>
      <div class="text-center space-y-4">
        <h3 class="text-2xl font-semibold">¡Bienvenida! 💄</h3>
        <p class="text-sm text-zinc-600">
          Suscribite y obtené un <strong class="text-pink-600">10% OFF</strong> en tu primera
          compra
        </p>
        <input
          type="email"
          placeholder="Tu email"
          class="w-full px-4 py-3 rounded-full border border-zinc-200 text-sm focus:outline-none focus:border-pink-300"
        />
        <button class="w-full px-4 py-3 rounded-full bg-pink-600 text-white text-sm font-medium hover:bg-pink-700 transition-colors">
          Quiero mi descuento
        </button>
        <p class="text-xs text-zinc-500">*Aplicable en compras mayores a $20.000</p>
      </div>
    </div>
  </div>

  {/* HERO principal */}
  <section class="grid gap-10 md:grid-cols-2 items-center mb-16">
    <div class="space-y-4">
      <p class="text-[11px] uppercase tracking-[0.2em] text-pink-500 mb-10">
        Nueva colección · Skin ritual
      </p>
      <h1 class="text-3xl md:text-4xl font-semibold leading-tight mb-10">
        Cuidar tu piel puede ser simple, suave y consciente.
      </h1>
      <p class="text-sm text-zinc-600 max-w-md mb-10">
        Maquillaje de alta calidad, cruelty-free y formulado para todo tipo de pieles. Realza tu
        belleza natural con productos que cuidan tu piel.
      </p>
      <div class="flex flex-wrap gap-3">
        <a
          href="/products"
          class="inline-flex items-center justify-center px-4 py-2 rounded-full bg-pink-600 text-white text-xs md:text-sm font-medium hover:bg-pink-700 transition-colors"
        >
          Ver todos los productos
        </a>
      </div>
    </div>

    <div class="relative">
      <div class="aspect-[4/5] rounded-[2.5rem] bg-gradient-to-b from-pink-100 via-rose-50 to-amber-50 shadow-md">
        <img
          src="/Hero_Principal/Hero_Principal_1.webp"
          alt="Maquillaje"
          class="rounded-[2.5rem]"
        />
      </div>
      <div class="absolute -bottom-4 -left-4 hidden md:block rounded-3xl bg-white shadow-lg px-4 py-3 text-xs">
        <p class="font-semibold">Ritual diario</p>
        <p class="text-zinc-500">Limpieza · Sérum · Crema · Contorno</p>
      </div>
    </div>
  </section>

  {/* TRUST BADGES / BENEFICIOS */}
  <section class="mb-16">
    <div class="grid grid-cols-2 md:grid-cols-4 gap-4">
      <div class="text-center p-4 rounded-2xl bg-pink-50/50 border border-pink-100 shadow-lg">
        <div class="text-2xl mb-2">🐰</div>
        <p class="text-xs font-medium">Cruelty Free</p>
        <p class="text-[10px] text-zinc-500">100% libre de crueldad</p>
      </div>
      <div class="text-center p-4 rounded-2xl bg-pink-50/50 border border-pink-100 shadow-lg">
        <div class="text-2xl mb-2">🚚</div>
        <p class="text-xs font-medium">Envío Gratis</p>
        <p class="text-[10px] text-zinc-500">Compras +$30.000</p>
      </div>
      <div class="text-center p-4 rounded-2xl bg-pink-50/50 border border-pink-100 shadow-lg">
        <div class="text-2xl mb-2">🔒</div>
        <p class="text-xs font-medium">Pago Seguro</p>
        <p class="text-[10px] text-zinc-500">Protección garantizada</p>
      </div>
      <div class="text-center p-4 rounded-2xl bg-pink-50/50 border border-pink-100 shadow-lg">
        <div class="text-2xl mb-2">↩️</div>
        <p class="text-xs font-medium">Devoluciones</p>
        <p class="text-[10px] text-zinc-500">30 días sin preguntas</p>
      </div>
    </div>
  </section>

  {/* NUESTROS PRODUCTOS - CARRUSEL */}
  <section class="space-y-4 mb-14">
    <div
      class="flex flex-col items-center text-center gap-3 md:flex-row md:items-baseline md:justify-between md:text-left"
    >
      <h2 class="text-lg md:text-xl font-semibold">Nuestros productos</h2>
      <div class="flex flex-wrap gap-2 text-[11px] md:text-xs text-zinc-500">
        <button class="px-3 py-1 rounded-full border border-zinc-900 text-zinc-900">Todos</button>
        <button
          class="px-3 py-1 rounded-full border border-zinc-200 hover:border-pink-300 hover:text-pink-700 transition-colors"
        >
          Limpiar
        </button>
        <button
          class="px-3 py-1 rounded-full border border-zinc-200 hover:border-pink-300 hover:text-pink-700 transition-colors"
        >
          Nutrir
        </button>
        <button
          class="px-3 py-1 rounded-full border border-zinc-200 hover:border-pink-300 hover:text-pink-700 transition-colors"
        >
          Juventud
        </button>
      </div>
    </div>

    <div class="relative">
      <button
        type="button"
        data-carousel-prev
        class="hidden md:flex absolute left-0 top-1/2 -translate-y-1/2 -translate-x-1/2 z-10 h-9 w-9 items-center justify-center rounded-full border border-zinc-300 bg-white/90 text-zinc-700 shadow-sm hover:border-pink-300 hover:text-pink-700 transition-colors"
      >
        ‹
      </button>

      <div
        data-carousel="products"
        class="flex gap-4 overflow-x-auto pb-3 pt-1 snap-x snap-mandatory scroll-smooth"
      >
        {featuredProducts.map((product) => (
          <article
            class="min-w-[220px] max-w-[230px] snap-start rounded-3xl border bg-white p-4 flex flex-col gap-3 hover:shadow-md hover:border-pink-200 transition-all"
          >
            <div class="relative">
              <div
                class="w-full h-64 rounded-2xl bg-pink-50 mb-3 bg-cover bg-center"
                style={`background-image: url('${product.image}');`}
              />
              <button
                class="absolute top-2 right-2 w-8 h-8 rounded-full bg-white/80 backdrop-blur-sm flex items-center justify-center text-pink-500 hover:bg-white hover:scale-110 transition-all"
              >
                ♡
              </button>
            </div>
            <div class="space-y-1">
              <p class="text-[11px] uppercase tracking-[0.16em] text-zinc-500">
                {product.category}
              </p>
              <h3 class="text-sm font-medium">{product.name}</h3>
              <p class="text-xs text-zinc-500">{product.description}</p>
            </div>
            <div class="mt-auto flex items-center justify-between pt-2">
              <span class="text-sm font-semibold text-pink-700">{product.price}</span>
              <a
                href="/cart"
                class="text-[11px] px-3 py-1 rounded-full bg-zinc-900 text-zinc-50 hover:bg-pink-700 transition-colors"
              >
                Agregar al carrito
              </a>
            </div>
          </article>
        ))}
      </div>

      <button
        type="button"
        data-carousel-next
        class="hidden md:flex absolute right-0 top-1/2 -translate-y-1/2 translate-x-1/2 z-10 h-9 w-9 items-center justify-center rounded-full border border-zinc-300 bg-white/90 text-zinc-700 shadow-sm hover:border-pink-300 hover:text-pink-700 transition-colors"
      >
        ›
      </button>
    </div>
  </section>

  {/* BUSCADOR DE TONOS */}
  <section class="mb-16 rounded-3xl bg-gradient-to-br from-pink-50 to-rose-50 border border-pink-100 p-8">
    <div class="max-w-2xl mx-auto text-center space-y-6">
      <h2 class="text-xl md:text-2xl font-semibold">Encontrá tu tono perfecto</h2>
      <p class="text-sm text-zinc-600">
        Respondé algunas preguntas y te ayudamos a elegir el mejor producto para tu piel
      </p>

      <div class="grid gap-4 md:grid-cols-3 text-left">
        <div class="bg-white rounded-2xl p-4 space-y-2 shadow-lg">
          <label class="text-xs font-medium text-zinc-700">Tipo de piel</label>
          <select
            class="w-full px-3 py-2 rounded-lg border border-zinc-200 text-sm focus:outline-none focus:border-pink-300"
          >
            <option>Seleccionar</option>
            <option>Seca</option>
            <option>Grasa</option>
            <option>Mixta</option>
            <option>Normal</option>
          </select>
        </div>

        <div class="bg-white rounded-2xl p-4 space-y-2 shadow-lg">
          <label class="text-xs font-medium text-zinc-700">Tono de piel</label>
          <select
            class="w-full px-3 py-2 rounded-lg border border-zinc-200 text-sm focus:outline-none focus:border-pink-300"
          >
            <option>Seleccionar</option>
            <option>Muy claro</option>
            <option>Claro</option>
            <option>Medio</option>
            <option>Oscuro</option>
          </select>
        </div>

        <div class="bg-white rounded-2xl p-4 space-y-2 shadow-lg">
          <label class="text-xs font-medium text-zinc-700">Subtono</label>
          <select
            class="w-full px-3 py-2 rounded-lg border border-zinc-200 text-sm focus:outline-none focus:border-pink-300"
          >
            <option>Seleccionar</option>
            <option>Cálido</option>
            <option>Frío</option>
            <option>Neutro</option>
          </select>
        </div>
      </div>

      <button class="px-6 py-3 rounded-full bg-pink-600 text-white text-sm font-medium hover:bg-pink-700 transition-colors">
        Ver mis productos recomendados
      </button>
    </div>
  </section>

  {/* LOOKS E INSPIRACIÓN */}
  <section class="mb-16 space-y-4">
    <div class="flex items-baseline justify-between gap-4">
      <h2 class="text-lg md:text-xl font-semibold">Inspiración & Looks</h2>
      <a
        href="/looks"
        class="text-[11px] md:text-xs uppercase tracking-[0.18em] text-zinc-600 hover:text-pink-700"
      >
        Ver todos
      </a>
    </div>

    <div class="grid gap-4 md:grid-cols-3">
      {looks.map((look) => (
        <article
          class="group relative rounded-3xl overflow-hidden bg-gradient-to-br from-pink-100 to-rose-100 aspect-[3/4] hover:shadow-lg transition-all"
        >
          <div
            class="absolute inset-0 bg-cover bg-center"
            style={`background-image: url('${look.image}');`}
          ></div>

          <div class="absolute inset-0 bg-black/20 group-hover:bg-black/60 transition-all duration-300"></div>

          <div class="absolute bottom-0 left-0 right-0 p-6 text-white space-y-2 z-10">
            <h3 class="text-lg font-semibold">{look.title}</h3>
            <p class="text-xs opacity-90">{look.description}</p>
            <p class="text-[11px] uppercase tracking-wider text-pink-200">{look.products}</p>
            <button
              class="mt-2 text-xs px-4 py-2 rounded-full bg-white/20 backdrop-blur-sm hover:bg-white/30 transition-colors"
            >
              Ver tutorial →
            </button>
          </div>
        </article>
      ))}
    </div>
  </section>

{/* NUESTRAS COLECCIONES */}
<section id="collections" class="mb-14 scroll-mt-40">
  <div class="flex items-baseline justify-between gap-4 mb-4">
    <h2 class="text-lg md:text-xl font-semibold">Nuestras colecciones</h2>
    <a
      href="/products"
      class="text-[11px] md:text-xs uppercase tracking-[0.18em] text-zinc-600 hover:text-pink-700"
    >
      Ver todo
    </a>
  </div>

  <div class="grid gap-4 md:grid-cols-3">
    {collections.map((collection) => (
      <article
        class="group rounded-3xl bg-white border border-zinc-200 hover:border-pink-200 hover:shadow-md transition-all p-5 flex flex-col justify-between"
      >
        <div class="space-y-1">
          <h3 class="text-sm font-semibold">{collection.name}</h3>
          <p class="text-xs text-zinc-500">{collection.description}</p>
        </div>
        <div class="mt-4 flex items-center justify-between text-[11px] text-pink-700">
          <span class="uppercase tracking-[0.16em]">Discover</span>
          <span class="group-hover:translate-x-1 transition-transform">→</span>
        </div>
      </article>
    ))}
  </div>
</section>


  {/* SOBRE NOSOTROS */}
  <section class="mb-16 rounded-3xl bg-white border border-zinc-200 overflow-hidden">
    <div class="grid md:grid-cols-2 gap-8 items-center">
      <div class="order-2 md:order-1 p-8 md:p-12 space-y-4">
        <p class="text-[11px] uppercase tracking-[0.2em] text-pink-500 mb-10">Nuestra historia</p>
        <h2 class="text-2xl md:text-3xl font-semibold">Belleza que respeta tu piel y el planeta</h2>
        <p class="text-sm text-zinc-600 leading-relaxed mb-7">
          Creemos que el maquillaje debe realzar tu belleza natural sin comprometer tu salud ni el
          medio ambiente. Por eso, todos nuestros productos son cruelty-free, veganos y formulados
          con ingredientes de origen natural.
        </p>
        <div class="space-y-2 pt-2">
          <div class="flex items-center gap-2 text-sm">
            <span class="text-pink-500">✓</span>
            <span>100% libre de crueldad animal</span>
          </div>
          <div class="flex items-center gap-2 text-sm">
            <span class="text-pink-500">✓</span>
            <span>Ingredientes naturales y veganos</span>
          </div>
          <div class="flex items-center gap-2 text-sm">
            <span class="text-pink-500">✓</span>
            <span>Packaging sustentable y reciclable</span>
          </div>
        </div>
        <a
          href="/about"
          class="inline-block mt-4 text-xs uppercase tracking-wider text-pink-600 hover:text-pink-700 mb-10"
        >
          Conocé más sobre nosotros →
        </a>
      </div>
      <div class="order-1 md:order-2 aspect-square bg-gradient-to-br from-pink-200 to-rose-100">
        <img src="/Sobre_Nosotros/Sobre_Nosotros_1.webp" alt="Maquillaje" />
      </div>
    </div>
  </section>

  {/* INSTAGRAM FEED */}
  <section class="mb-16 space-y-4">
    <div class="text-center space-y-2">
      <h2 class="text-lg md:text-xl font-semibold">Seguinos en Instagram</h2>
      <p class="text-sm text-zinc-600">
        Compartí tu look con{" "}
        <span class="text-pink-600 font-medium">#MiRitualDeBeauty</span>
      </p>
    </div>

    <div class="grid grid-cols-2 md:grid-cols-4 gap-3">
      {instagramPosts.map((post) => {
        const isVideo = post.video.endsWith(".mp4");

        return (
          <a
            href="https://instagram.com"
            target="_blank"
            class="group relative aspect-square rounded-2xl bg-gradient-to-br from-pink-100 to-rose-100 overflow-hidden"
          >
            {isVideo ? (
              <video
                src={post.video}
                muted
                autoplay
                loop
                playsinline
                class="absolute inset-0 h-full w-full object-cover"
              ></video>
            ) : (
              <img src={post.video} class="absolute inset-0 h-full w-full object-cover" />
            )}

            <div class="absolute inset-0 bg-black/0 group-hover:bg-black/30 transition flex items-center justify-center">
              <span class="opacity-0 group-hover:opacity-100 text-white text-xs">
                ♥ {post.likes}
              </span>
            </div>
          </a>
        );
      })}
    </div>

    <div class="text-center pt-2">
      <a
        href="https://instagram.com"
        target="_blank"
        class="inline-flex items-center gap-2 px-5 py-2 rounded-full border border-pink-200 text-xs text-pink-700 hover:bg-pink-50 transition-colors"
      >
        <span>📷</span>
        <span>Seguir @tutienda</span>
      </a>
    </div>
  </section>

  {/* RESEÑAS / TESTIMONIOS */}
  <section class="mb-16 space-y-4">
    <h2 class="text-lg md:text-xl font-semibold">Lo que dicen nuestras clientas</h2>
    <div class="grid gap-4 md:grid-cols-3">
      {reviews.map((review) => (
        <article class="rounded-3xl bg-white border border-zinc-200 p-5 flex flex-col gap-3">
          <div class="flex items-center justify-between gap-2">
            <div>
              <p class="text-sm font-medium">{review.name}</p>
              <p class="text-[11px] text-zinc-500">{review.role}</p>
            </div>
            <div class="flex gap-0.5 text-amber-500 text-xs">
              {Array.from({ length: 5 }).map((_, i) => (
                <span>{i < review.rating ? "★" : "☆"}</span>
              ))}
            </div>
          </div>
          <p class="text-xs text-zinc-600 leading-relaxed">"{review.text}"</p>
        </article>
      ))}
    </div>
  </section>

  {/* BLOG */}
  <section class="mb-16 space-y-4">
    <div class="flex items-baseline justify-between gap-4">
      <h2 class="text-lg md:text-xl font-semibold">Tips & Tendencias</h2>
      <a
        href="/blog"
        class="text-[11px] md:text-xs uppercase tracking-[0.18em] text-zinc-600 hover:text-pink-700"
      >
        Ver blog
      </a>
    </div>

    <div class="grid gap-4 md:grid-cols-3">
      {blogPosts.map((post) => (
        <article
          class="group rounded-3xl bg-white border border-zinc-200 hover:border-pink-200 hover:shadow-md transition-all overflow-hidden shadow-lg"
        >
          <div
            class="aspect-video bg-gradient-to-br from-pink-100 to-rose-100 bg-cover bg-center"
            style={`background-image: url('${post.image}');`}
          />
          <div class="p-5 space-y-2">
            <p class="text-[10px] uppercase tracking-wider text-pink-600">{post.category}</p>
            <h3 class="text-sm font-medium group-hover:text-pink-700 transition-colors">
              {post.title}
            </h3>
            <div class="flex items-center justify-between pt-2">
              <p class="text-[11px] text-zinc-500">{post.date}</p>
              <span class="text-[11px] text-pink-600 group-hover:translate-x-1 transition-transform">
                Leer más →
              </span>
            </div>
          </div>
        </article>
      ))}
    </div>
  </section>

  {/* UBICACIÓN */}
  <section class="mb-16 space-y-4">
    <h2 class="text-lg md:text-xl font-semibold">Nuestra Ubicación</h2>
    <div class="w-full">
      <div class="grid gap-6 md:grid-cols-2 items-start">
        <div class="w-full">
          <iframe
            src="https://www.google.com/maps/embed?pb=!1m18!1m12!1m3!1d27171.672023392897!2d-63.925232841568885!3d-31.648659762377488!2m3!1f0!2f0!3f0!3m2!1i1024!2i768!4f13.1!3m3!1m2!1s0x9432cafff1f1cec1%3A0xf370c1a41d5322ac!2zUsOtbyBTZWd1bmRvLCBDw7NyZG9iYQ!5e0!3m2!1ses!2sar!4v1763138129136!5m2!1ses!2sar"
            class="w-full h-64 md:h-96 rounded-2xl"
            style="border:0;"
            allowfullscreen
            loading="lazy"
            referrerpolicy="no-referrer-when-downgrade"
          >
          </iframe>
        </div>

        <div class="w-full space-y-6">
          <div class="space-y-3">
            <h3 class="text-sm font-semibold">Nuestras tiendas</h3>
            <div class="space-y-3">
              <div
                class="p-4 rounded-2xl bg-pink-50/50 border border-pink-100 shadow-lg text-center"
              >
                <p class="text-sm font-medium mb-1">📍 Showroom Centro</p>
                <p class="text-xs text-zinc-600">Av. Principal 1234, Río Segundo</p>
                <p class="text-xs text-zinc-500 mt-1">Lun-Vie: 10-19hs | Sáb: 10-14hs</p>
              </div>
              <div
                class="p-4 rounded-2xl bg-pink-50/50 border border-pink-100 shadow-lg text-center"
              >
                <p class="text-sm font-medium mb-1">📍 Local Shopping</p>
                <p class="text-xs text-zinc-600">Shopping Local, Local 45</p>
                <p class="text-xs text-zinc-500 mt-1">Lun-Dom: 10-21hs</p>
              </div>
            </div>
          </div>

          <div
            class="p-4 rounded-2xl bg-zinc-50 border border-zinc-200 shadow-lg text-center"
          >
            <h4 class="text-sm font-medium mb-2">Información de envíos</h4>
            <ul class="space-y-1 text-xs text-zinc-600">
              <li>• Envíos gratis en compras +$30.000</li>
              <li>• Entrega 3-5 días hábiles</li>
              <li>• Retiro gratis en tienda</li>
            </ul>
          </div>
        </div>
      </div>
    </div>
  </section>

  {/* Script del carrusel y popup */}
  <script>
    if (typeof window !== "undefined") {
      const container = document.querySelector('[data-carousel="products"]');
      const prevBtn = document.querySelector("[data-carousel-prev]");
      const nextBtn = document.querySelector("[data-carousel-next]");
      const scrollAmount = 260;

      prevBtn?.addEventListener("click", () => {
        container?.scrollBy({ left: -scrollAmount, behavior: "smooth" });
      });

      nextBtn?.addEventListener("click", () => {
        container?.scrollBy({ left: scrollAmount, behavior: "smooth" });
      });

      const popup = document.getElementById("newsletter-popup");
      const closeBtn = document.getElementById("close-popup");

      setTimeout(() => {
        popup?.classList.remove("hidden");
      }, 3000);

      closeBtn?.addEventListener("click", () => {
        popup?.classList.add("hidden");
      });

      popup?.addEventListener("click", (e) => {
        if (e.target === popup) {
          popup.classList.add("hidden");
        }
      });

      const wishlistBtns = document.querySelectorAll(
        'button[class*="absolute top-2 right-2"]'
      );
      wishlistBtns.forEach((btn) => {
        btn.addEventListener("click", () => {
          btn.textContent = btn.textContent === "♡" ? "♥" : "♡";
        });
      });
    }
  </script>

  <style>
    @keyframes fade-in {
      from {
        opacity: 0;
        transform: scale(0.95);
      }
      to {
        opacity: 1;
        transform: scale(1);
      }
    }

    .animate-fade-in {
      animation: fade-in 0.3s ease-out;
    }

    [data-carousel="products"] {
      scrollbar-width: none;
      -ms-overflow-style: none;
    }

    [data-carousel="products"]::-webkit-scrollbar {
      display: none;
    }
  </style>
</MainLayout>
