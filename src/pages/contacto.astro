---
import Header from "../layouts/Header.astro";
import Footer from "../layouts/Footer.astro";
import MainLayout from "../layouts/MainLayout.astro";
---

<MainLayout
    title="Contacto – Alvit Eclat | Consultas, Asesoramiento y Atención Personalizada"
    description="¿Tenés dudas sobre nuestros productos o necesitás asesoramiento personalizado? Contactanos y nuestro equipo te ayudará a encontrar la rutina de cuidado facial ideal para tu piel."
>
    <Header />

    <section class="bg-gradient-to-b from-gray-50 to-white py-16 px-4 sm:px-6 lg:px-8">
        <div class="max-w-7xl mx-auto">
            <!-- Header -->
            <div class="text-center mb-16">
                <h1 class="text-5xl md:text-6xl font-bold text-gray-900 mb-4 text-transparent bg-clip-text bg-gradient-to-r from-pink-500 to-purple-600">
                    Nos Encantaría Escucharte
                </h1>
                <p class="text-xl text-gray-600 font-light">contáctanos</p>
            </div>

            <div class="grid grid-cols-1 lg:grid-cols-2 gap-12 items-start">
                <!-- Formulario -->
                <div class="bg-white rounded-2xl shadow-xl p-8 md:p-10">
                    <p class="text-gray-700 mb-8 text-lg">
                        Estaremos encantados de contactarte lo antes posible.<br />
                        ¡Deja tus datos a continuación y te responderemos pronto!
                    </p>

                    <!-- ✅ Formulario para EmailJS -->
                    <form id="contact-form" class="space-y-6">
                        <div>
                            <input
                                type="text"
                                name="from_name"
                                placeholder="Nombre completo"
                                class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent outline-none transition"
                                required
                            />
                        </div>

                        <div>
                            <input
                                type="email"
                                name="email"
                                placeholder="Correo electrónico"
                                class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent outline-none transition"
                                required
                            />
                        </div>

                        <div>
                            <input
                                type="tel"
                                name="from_phone"
                                placeholder="Teléfono"
                                class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent outline-none transition"
                            />
                        </div>

                        <div>
                            <textarea
                                name="message"
                                rows="5"
                                placeholder="Tu mensaje"
                                class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent outline-none transition resize-none"
                                required
                            ></textarea>
                        </div>

                        <button
                            type="submit"
                            class="w-full bg-pink-600 text-black px-5 py-3 uppercase rounded-lg hover:bg-pink-800 hover:text-white transition-colors border border-black text-sm sm:text-base text-center"
                        >
                            Enviar Mensaje
                        </button>
                    </form>
                </div>

                <!-- Información de contacto -->
                <div class="space-y-8 text-center">
                    <!-- Expertos en tu área -->
                    <div class="bg-gradient-to-br from-blue-50 to-indigo-50 rounded-2xl p-8 border border-blue-100">
                        <h2 class="text-2xl font-bold text-gray-900 mb-4">
                            Atención Experta en Dermocuidado Avanzado
                        </h2>
                        <p class="text-gray-700 leading-relaxed">
                            Nuestros especialistas trabajan con enfoques clínicos para tratar acné, envejecimiento cutáneo, melasma, sensibilidad y protección solar de amplio espectro. Recibirás recomendaciones precisas según las necesidades biológicas de tu piel.
                        </p>
                    </div>

                    <!-- Detalles de contacto -->
                    <div class="grid grid-cols-1 sm:grid-cols-2 gap-6">
                        <!-- Llamanos -->
                        <div class="bg-white rounded-xl shadow-md p-6 hover:shadow-lg transition">
                            <div class="text-pink-600 mb-3 flex justify-center items-center">
                                <svg class="w-8 h-8" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z"></path>
                                </svg>
                            </div>
                            <h3 class="font-semibold text-gray-900 mb-2">Llámanos</h3>
                            <p class="text-gray-600">+54 11 1234-5678</p>
                        </div>

                        <!-- Email -->
                        <div class="bg-white rounded-xl shadow-md p-6 hover:shadow-lg transition">
                            <div class="text-pink-600 mb-3 flex justify-center items-center">
                                <svg class="w-8 h-8" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"></path>
                                </svg>
                            </div>
                            <h3 class="font-semibold text-gray-900 mb-2">Escríbenos</h3>
                            <p class="text-gray-600">info@tuempresa.com</p>
                        </div>

                        <!-- Horarios -->
                        <div class="bg-white rounded-xl shadow-md p-6 hover:shadow-lg transition">
                            <div class="text-pink-600 mb-3 flex justify-center items-center">
                                <svg class="w-8 h-8" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                                </svg>
                            </div>
                            <h3 class="font-semibold text-gray-900 mb-2">Horarios</h3>
                            <p class="text-gray-600">Lunes-Viernes<br />09:00-17:00</p>
                        </div>

                        <!-- Ubicación -->
                        <div class="bg-white rounded-xl shadow-md p-6 hover:shadow-lg transition">
                            <div class="text-pink-600 mb-3 flex justify-center items-center">
                                <svg class="w-8 h-8" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z"></path>
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z"></path>
                                </svg>
                            </div>
                            <h3 class="font-semibold text-gray-900 mb-2">Encuéntranos</h3>
                            <p class="text-gray-600">Av. Corrientes 1234<br />Buenos Aires</p>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <Footer />
</MainLayout>

<!-- 📨 LÓGICA DE EMAILJS -->
<script type="module">
  import emailjs from "@emailjs/browser";

  // 👇 Reemplazá estos tres por los valores reales de tu cuenta EmailJS
  const SERVICE_ID = "service_ajn2u06";     // ej: "service_abcd123"
  const TEMPLATE_ID = "template_6e3r3rd";   // ej: "template_xxx123"
  const PUBLIC_KEY  = "gcbEB2wW1kLoICuTX";    // ej: "Q1wErTy..."

  window.addEventListener("DOMContentLoaded", () => {
    const form = document.getElementById("contact-form");

    if (!form) {
      console.error("No se encontró el formulario con id #contact-form");
      return;
    }

    console.log("✅ Listener de submit conectado al formulario");

    form.addEventListener("submit", (event) => {
      event.preventDefault();

      console.log("⌛ Enviando formulario a EmailJS...");

      emailjs
        .sendForm(SERVICE_ID, TEMPLATE_ID, form, PUBLIC_KEY)
        .then(() => {
          console.log("✅ EmailJS: mensaje enviado correctamente");
          alert("✅ Tu mensaje fue enviado correctamente. ¡Gracias por escribirnos!");
          form.reset();
        })
        .catch((error) => {
          console.error("❌ Error al enviar el mensaje con EmailJS:", error);
          alert("❌ Ocurrió un problema al enviar el mensaje. Revisá la consola del navegador.");
        });
    });
  });
</script>
